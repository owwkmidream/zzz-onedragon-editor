{
  "name": "charge-plan-editor-tauri",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:win": "pnpm tauri build --bundles nsis msi && node ./scripts/package-portable.mjs",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "lucide": "^0.577.0",
    "sortablejs": "^1.15.7"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/sortablejs": "^1.15.9",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
